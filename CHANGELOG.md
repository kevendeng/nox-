# 更新日志 / Changelog

本脚本是一个 Tampermonkey 油猴脚本，用于在 NoxInfluencer（cn.noxinfluencer.com）
上批量处理达人营销的重复操作：批量录入关键词、批量创建收藏夹、批量收藏达人、
邮件项目自动化、CRM 筛选全选。

版本号沿用脚本开发过程中的连续编号；`8.6` 起纳入 Git 版本管理，之前的版本为
本地迭代（下方“早期演进”一节说明）。

---

## [8.8] - 2026-07-30

搜索页收藏功能的架构性重构收尾，达到三平台可用状态。

### 新增
- **三平台自动支持**：Instagram、YouTube、TikTok。脚本从当前搜索页网址
  （`/search/<平台>/`）自动识别平台，用户无需手动选择或输入平台编号。
- **实时进度显示**：面板底部新增状态行，运行时显示当前第几页、本页收藏数、
  累计进度（如“已收藏 320/500（第7页 +12）”）。
- 收藏结束后自动刷新收藏夹列表，"已装人数"即时更新。

### 变更
- 收藏夹下拉从显示"剩余容量"改为显示**"已装 N 人"**（= 5000 上限 − 剩余），
  更符合"我已经往这个夹里放了多少人"的实际关注点。
- 删除平台编号输入框（改为自动识别）。

### 修复
- 修正 channelId 提取正则：YouTube 的达人 ID 是 `UCxxx` 字母串而非纯数字，
  旧正则 `\d+` 只匹配数字，导致在 YouTube 页收藏 0 个。改为 `[^/?#]+`，
  同时兼容 Instagram/TikTok 的纯数字 ID 和 YouTube 的字母串 ID。

---

## [8.7] - 2026-07-30

搜索页收藏的**核心重构**：从"模拟界面操作"转向"直接调用接口"。

### 背景问题
旧方案靠"点全选 → 翻页 → 网站跨页累计选中 → 手动加入收藏夹"。当累计到
四千多个选中状态时，前端 Vue 渲染被拖垮，列表变空、全选框消失，脚本在约
第 43 页超时自停，无法完成上万量级的收藏任务。

### 解决方案
- 绕过界面，直接抓取每页达人的 channelId，调用收藏接口（`PUT /ws/collection`）
  批量提交，翻页后重复。页面永远只需渲染当前一页，不再累积选中状态，
  根除了渲染崩溃。
- **正确排除隐藏达人**：已建联/已合作的达人在页面上被隐藏（带
  `youtube-channel-fade` 类且不可见），但仍存在于 DOM 中。脚本只抓取
  真正可见的卡片（`:not(.youtube-channel-fade)` 且 `offsetParent !== null`），
  确保不会误收建联过的达人。
- **智能等待渲染**：每页等待"可见达人数量连续稳定"后再抓取，避免在隐藏标记
  尚未渲染时抢跑导致误收。

### 新增
- 收藏夹选择改为**按名字下拉多选**（此前需手填数字 ID，用户无从得知 ID 对应
  哪个夹）。通过 `simpleGroupList` 接口拉取全部收藏夹，支持按名字过滤
  （如输入日期前缀 `0730` 快速定位当天新建的夹）、多选、手动刷新。
- 目标数量 + 收够即停。

---

## [8.6] - 2026-07-26

首个纳入 Git 版本管理的版本，脚本功能已趋于完整。

### 新增
- 上传至 GitHub，脚本头部配置 `@updateURL`，支持在 Tampermonkey 中一键检查更新。
- 编写 README，包含安装指南与功能介绍（关键词录入、自动全选、批量建收藏夹等），
  并补充操作截图。

### 修复
- 统一版本号为单一常量：以后升级只改一处，面板标题与日志自动同步，
  避免出现"脚本头写 8.6、面板却显示 8.5"的不一致。

### 此时已具备的功能（四大面板，按页面自动切换）
- **搜索页**：批量录入关键词、逐页全选 + 自动翻页。
- **收藏夹页**：按"日期前缀 + 编号"批量创建收藏夹，支持指定协作者与可见范围。
- **邮件页**：一键创建邮件项目、自动添加收件人、设置次日零点定时发送。
- **CRM 页**：自动筛选"建联中"状态并跨页全选。

---

## 早期演进（Git 之前的本地迭代，约 v1 – v8.5）

这些版本未纳入本仓库，以下为开发过程的概述：

- **起点**：解决"粘贴一长串关键词时被网站当成单个词"的问题，最初做成一个
  `javascript:` 书签，点击后弹框粘贴关键词、自动逐个录入。
- **关键技术踩坑**：
  - 每录入一个词需重新查询输入框（组件重渲染会让旧引用失效），否则只有第一个词生效。
  - 加词需用原生 value setter + input 事件 + 模拟回车（keyCode 13）组合触发。
  - 必须在正常浏览模式下运行，开发者工具的手机/响应式模拟模式会改变 DOM 结构导致失败。
- **形态演进**：从书签脚本 → 油猴悬浮面板（曾因面板挂载报错、被模拟模式干扰反复受挫）
  → 稳定的多面板油猴脚本。
- **功能扩张**：逐步加入批量创建收藏夹、邮件项目自动化、CRM 筛选全选，
  形成 8.6 版的四大面板体系。

---

## 演进主线小结

一条清晰的成熟化路径：

1. **让功能跑起来**（早期 – 8.5）：从单一书签到多功能面板，在真实页面上反复踩坑、调稳。
2. **纳入版本管理**（8.6）：上 GitHub、写文档、加自动更新。
3. **架构性重构**（8.7 – 8.8）：把最脆弱的"依赖界面渲染"环节，换成稳定的接口直调，
   从"能用但会崩"进化到"可无人值守跑上万量级"。
